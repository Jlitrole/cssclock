const second hand = document.querySelector('.second-hand');

function setDate() {
  const now = new Date();
  const seconds = now.getSeconds();
  const secondDegrees = ((econds / 60) * 360);
  
  console.log(seconds);
  }
  
  SetInterval(setDate, 1000);
  
  </script>
  </body>
  </html>
